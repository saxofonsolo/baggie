import { Example } from "./Example";

# getPercentageBetween

Get the position between two numbers as a percentage.

## Parameters

| Parameter    | Type     | Required |
| :----------- | :------- | :------: |
| **from**     | `number` |    ✔     |
| **to**       | `number` |    ✔     |
| **position** | `number` |    ✔     |

## Examples

<Example from={0} to={10} position={5} />

<Example from={10} to={20} position={20} />

<Example from={-1} to={1} position={0} />
