import { Example } from "./Example";

# isNumberRound

Check if a number has the desired amount of decimal places.

## Example

Check for **two** decimals.

<Example number={13.37} maxDecimalPlaces={2} />

## Example

Check for **three** decimals.

<Example number={13.3751} maxDecimalPlaces={3} />
